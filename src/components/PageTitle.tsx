export const PageTitle = ({ title }: { title: string }) => (
  <div className="text-left sm:px-5 text-5xl sm:text-8xl text-gray-900 dark:text-white font-semibold title-font mb-2 capitalize">
    {title}
  </div>
);
